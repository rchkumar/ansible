---
- hosts: dbserver
  tasks:
    - name: install a package
      yum:
        name: "{{package_name}}"
        state: present
    - name: start the service
      service:
        name: "{{package_name}}"
        state: started